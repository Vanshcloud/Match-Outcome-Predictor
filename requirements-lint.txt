# Lint and type tooling, PINNED EXACTLY.
#
# Unpinned, CI installs whatever released that morning: a formatter release
# turns the build red with no code change, and disagrees with every local run.
# These versions are matched to the sibling Transfer Value Predictor repository
# so one toolchain covers both.
ruff==0.16.5
black==26.5.1
mypy==2.3.1

# Type stubs. Always preferred over a mypy `ignore_missing_imports` override:
# a stub type-checks the call, an override only silences it.
types-PyYAML==6.0.12.20260815
types-requests==2.33.0.20260712
# Matched to the INSTALLED pandas 2.3.x, not the latest release. pandas-stubs
# tracks pandas versions, so the 3.0.x stubs describe an API that pandas 2.3
# does not have — mypy would then be checking against the wrong library and
# reporting confident errors about methods that exist.
pandas-stubs==2.3.3.260113
# Matched to the installed scipy exactly, for the same reason as pandas-stubs:
# scipy-stubs tracks scipy versions, and stubs for a different minor describe
# an API the installed library does not have.
scipy-stubs==1.18.1.0
# plotly ships no py.typed marker; these are the community stubs
# and they earned their place immediately — they caught a `dict[str, object]`
# splatted into `update_layout`, which type-checks as nothing in particular and
# is how a misspelt layout key survives to be ignored at run time. Pinned
# exactly like the rest: at 0.1.x the API these describe is still moving.
#
# streamlit is deliberately absent from this file and from the mypy overrides:
# it ships its own py.typed, so it needs neither.
plotly-stubs==0.1.4
